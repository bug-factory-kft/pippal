"""PipPal Tier-2 user-journey E2E package.

Drives the REAL launched desktop app (a real pywebview WebView2 window
in the interactive session) over CDP. See ``conftest.py`` for the
two-tier model and the launch/attach technique, and ``README.md`` for
how to run it as the logged-in user.
"""
